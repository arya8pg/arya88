#include<stdio.h>
int main()
{
  int a,b;
  scanf("%d",&a);
  scanf("%d",&b);
  printf("before swapping the numbers are %d and %d",a,b);
  printf("after the swapping the numbers are %d and %d",b,a);
  return 0;
}